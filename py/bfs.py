

class IdGraph:

    def __init__(self):
        self.vertices = {}
    
    def link(p, q):

        vertices.get
        if p not in self.vertices.keys():
            self.vertices[p] = set([q]]
        else:
             self.vertices[p].add(q)
            if q not in self.vertices.keys():
                self.vertices[q] = []